{
  "name": "td-v1",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Mission Control — trading dashboard + read-only pathway into the AIOS Obsidian vault.",
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"node server/index.mjs\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build && node scripts/gen-precache.mjs",
    "start": "NODE_ENV=production node server/index.mjs",
    "setup": "npm install && npm --prefix web install",
    "test": "node --test web/src/**/*.test.ts",
    "framework:hashes": "node scripts/gen-framework-hashes.mjs",
    "sync:cloud": "node scripts/sync-cloud.mjs"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.21.2",
    "gray-matter": "^4.0.3",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
